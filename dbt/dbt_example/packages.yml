packages:
  # Shared macros (schema naming, query tag, run logging, run-metadata upload) and the shared
  # shared models/seeds (calendar, time, environment, unknown member).
  - local: ../dbt_common

  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
