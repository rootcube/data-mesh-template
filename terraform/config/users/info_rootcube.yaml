# yaml-language-server: $schema=../_validation/schemas/user.schema.json
# User: info@rootcube.com
# =======================
# One file per person. `create: false` for logins that already exist (SSO); `create: true`
# creates the user with a one-time password.

login: "info@rootcube.com"
name: "Mark Stellingwerff"
create: false
roles:
  - project: example
    role: engineer
    environments:
      - development
